<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TECH BLOG</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

</head>

<body>
    <header>
        <h1>Tech Blog</h1>
    </header>

    <nav class="nav fixed-top">
        <ul id="nav-list">
            <li>
                <a href="/home" class="icon-link"><i class="bi bi-currency-dollar"></i> Home</a>
            </li>
            <li>
                <a href="/dashboard" class="icon-link"><i class="bi bi-bar-chart-fill"></i> Dashboard</a>
            </li>
            <li>
                {{#if logged_in}}
                <button id="logout" action="/users/logout" method="POST"> <i class="bi bi-person-fill"></i>
                    Logout</button>
                {{/if}}
            </li>
        </ul>
    </nav>

    <main>
        {{{body}}}
    </main>
</body>

{{#if logged_in}}
<script src="./js/logout.js"></script>
{{/if}}
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js">
</script>

<script>
    var currentURL = window.location.pathname;

    var navList = document.getElementById("nav-list");

    var navLinks = navList.getElementsByTagName("a");

    for (var i = 0; i < navLinks.length; i++) {
        if (navLinks[i].getAttribute("href") === currentURL) {
            navLinks[i].classList.add("active");
        }
    }
</script>